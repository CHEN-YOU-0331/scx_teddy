# debug_classify 的監看清單
# 每行一個 comm 字串（會用 prefix 比對，所以 "stress-ng-" 可以匹配所有 stress-ng 子程序）
# '#' 開頭或空白行會被忽略

# 自訂 workload
fixed-mutex
slow-timer
random-timer

# stress-ng 家族（prefix 比對）
stress-ng-cpu
stress-ng-hdd
stress-ng-timer
stress-ng-switc
